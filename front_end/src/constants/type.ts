export type ProductType = {
    _id: string,
    category: string,
    description: string,
    image: string,
    isShow: boolean,
    price: number,
    rating: number,
    title: string,
    createdAt: string,
    updatedAt: string,
}